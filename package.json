{
  "name": "commerce-test-data",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "export:documents": "npm run build && node dist/document.js --export --doclists banners,hero_images,content_blocks ",
    "export:all": "npm run build && node dist/index.js --export:all ",
    "import:all": "npm run build && node dist/index.js --import:all ",
    "pretier": "prettier --write .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.7.1",
    "husky": "^7.0.4",
    "lint-staged": "^11.2.6",
    "nconf": "^0.11.3",
    "prettier": "2.4.1"
  },
  "dependencies": {
    "@jsonlines/core": "^1.0.2",
    "cli-spinner": "^0.2.10",
    "dotenv": "^10.0.0",
    "jsonlines": "^0.1.1",
    "mozu-node-sdk": "^2.15.7",
    "typescript": "^4.3.5"
  },
  "lint-staged": {
    "lib/*": "prettier --write ."
  }
}
